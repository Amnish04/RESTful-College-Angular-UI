import { ClickOnKeyDirective } from './click-on-key.directive';

describe('ClickOnKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOnKeyDirective();
    expect(directive).toBeTruthy();
  });
});
